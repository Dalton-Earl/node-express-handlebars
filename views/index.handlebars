<h1>Burger</h1>

<h2>Burgers you have eaten</h2>

<ul>
  {{#each burger}}
    {{#unless devoured}}
      {{> burger/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you want to eat</h2>

<ul>
  {{#each burger}}
    {{#if devoured}}
      {{> burger/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Enter a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgerName">Name of your burger</label>
    <input type="text" id="addedBurger" name="burger">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured</label><br>
    <input type="radio" name="devoured" value="1" checked>eaten <br>
    <input type="radio" name="devoured" value="0"> Not yet eaten
  </div>

  <button type="submit">Add a new burger</button>
</form>
