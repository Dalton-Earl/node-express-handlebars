<li>
	{{burger_name}}

	<button class="eatTheBurger btn" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Add back to the list{{else}}Go eat it!{{/if}}
	</button>

	<button class="deleteburger btn" data-id="{{this.id}}">DELETE!</button>
</li>
